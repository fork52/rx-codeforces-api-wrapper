package com.rxcodeforces.api.util;

/**
 * CommonConstants is a utility class that contains common constants used in the Codeforces API.
 */
public class CommonConstants {

  private CommonConstants() {
    // Private constructor to prevent instantiation
  }

  /**
   * Constant representing the contestId parameter.
   */
  public static final String CONTEST_ID_PARAM = "contestId";

  /**
   * Constant representing the count parameter.
   */
  public static final String COUNT_PARAM = "count";

  /**
   * Constant representing the handle parameter.
   */
  public static final String HANDLE_PARAM = "handle";

  /**
   * Constant representing the handles parameter.
   */
  public static final String HANDLES_PARAM = "handles";
}
